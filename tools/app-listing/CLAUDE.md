# Identity: tool · app-listing (CANONICAL)

**This is the canonical Listing Studio — edit it HERE.** Run `node ~/qdn/tools/sync-tools.mjs`
to push changes into each app's working copy (`apps/<app>/tools/app-listing`).

- **What:** standalone Astro + Playwright slide editor for Shopify App Store listing graphics.
- **Deep how-to:** the `applisting-design` skill (`.claude/skills/applisting-design/`).
- Workspace map → [`../../CLAUDE.md`](../../CLAUDE.md).
