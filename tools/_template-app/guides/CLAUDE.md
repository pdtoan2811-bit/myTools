# Identity: {{APP}} · GUIDES

**This folder = `TODO-org/TODO-repo` @ `TODO-branch` — the docs/guide hub.** Fill this in
when you clone the real repo here.

- **How content lands here:** authored by the **user-guide** tool (`../tools/user-guide`,
  skill `guide`) and published onto a `guide/<file>` branch for review. Register this app in
  the tool's `apps.config.mjs`.
- **Siblings:** the app it documents → `../app` (read-only), site → `../website`.
  App map → [`../CLAUDE.md`](../CLAUDE.md).
- **Scope:** only edit this repo. Prefer publishing through the tool over hand-writing `.mdx`.
