# Identity: tool · user-guide (CANONICAL)

**This is the canonical userGuideSnap guide factory — edit it HERE.** Run
`node ~/qdn/tools/sync-tools.mjs` to push changes into each app's working copy
(`apps/<app>/tools/user-guide`).

- **What:** captures a running localhost app with Playwright, renders branded annotated
  screenshots, assembles `guide.md`, and publishes `.mdx` into each app's guide repo.
- **Deep how-to:** the `guide` skill (`.claude/skills/guide/`); targets in `apps.config.mjs`.
- Workspace map → [`../../CLAUDE.md`](../../CLAUDE.md).
